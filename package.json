{
  "name": "trading-sync-app",
  "version": "3.6.0",
  "description": "Portfolio aggregation app that syncs data from multiple trading platforms via screenshot analysis",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "npm run build && cd backend && NODE_ENV=production node server.js",
    "seed": "cd backend && node -r dotenv/config scripts/seedDefaultUser.js"
  },
  "keywords": ["trading", "portfolio", "sync", "dashboard"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
